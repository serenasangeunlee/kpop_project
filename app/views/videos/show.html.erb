<div class="page-header">
</div>

<div class="row">
  <div class="col-md-12 mb-2">
    <dl class="dl-horizontal">
      <dt>User</dt>
      <dd><%= @video.user.username %></dd>
      <dt>Video</dt>
      <dd><iframe width="560" height="315" src="<%= @video.image %>" frameborder="0"></iframe></dd>
      <dt>Caption</dt>
      <dd><%= @video.caption %></dd>

    </dl>
    <div class="btn-group btn-group-justified">
      <a href="/videos" class="btn btn-primary">
        Back
      </a>
      <% if current_user==@video.user %>

      <a href="/videos/<%= @video.id %>/edit" class="btn btn-warning">
        Edit
      </a>
      <a href="/delete_video/<%= @video.id %>" class="btn btn-danger" rel="nofollow">
        Delete
      </a>
    <% end %>
    </div>
  </div>
</div>
